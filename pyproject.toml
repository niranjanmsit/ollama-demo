[project]
name = "ai-chatbot-ollama"
version = "0.1.0"
description = "A Python chatbot using local Ollama"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ollama>=0.1.0",
    "pydantic>=2.0.0",
]
